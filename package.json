{
  "name": "css-modules-repro",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build:browserify:actual": "cd browserify && yarn build-actual && yarn test",
    "build:browserify:expected": "cd browserify && yarn build-expected && yarn test",
    "build:parcel:actual": "cd parcel && yarn build-actual && yarn test",
    "build:parcel:expected": "cd parcel && yarn build-expected && yarn test",
    "build:rollup:actual": "cd rollup && yarn build-actual && yarn test",
    "build:rollup:expected": "cd rollup && yarn build-expected && yarn test",
    "build:webpack:actual": "cd webpack && yarn build-actual && yarn test",
    "build:webpack:expected": "cd webpack && yarn build-expected && yarn test"
  },
  "workspaces": [
    "browserify/",
    "parcel/",
    "rollup/",
    "webpack/",
    "packages/*"
  ]
}
